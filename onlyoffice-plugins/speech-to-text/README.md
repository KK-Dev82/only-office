# Speech To Text Plugin for OnlyOffice

Plugin สำหรับถอดเสียงพูดเป็นข้อความและ Insert ไปยังเอกสารใน OnlyOffice Editor

## Features

- 🎤 Speech Recognition ใช้ Web Speech API
- 📝 Insert text ไปยังตำแหน่ง cursor ปัจจุบัน
- ➕ Append text ที่ท้ายเอกสาร
- 🌐 รองรับหลายภาษา (ไทย, อังกฤษ)
- ✅ ทำงานภายใน Plugin โดยตรง (ไม่ต้องพึ่ง External Connector)

## การใช้งาน

1. เปิด Plugin จากเมนู Plugins ใน OnlyOffice
2. กดปุ่ม "เริ่มฟัง" เพื่อเริ่มการถอดเสียง
3. พูดข้อความที่ต้องการ
4. กดปุ่ม "หยุดฟัง" เมื่อเสร็จสิ้น
5. กดปุ่ม "Insert ไปยังเอกสาร" หรือ "Append ที่ท้ายเอกสาร" เพื่อเพิ่มข้อความลงในเอกสาร

## ข้อกำหนด

- Browser ต้องรองรับ Web Speech API (Chrome, Edge, Safari)
- ต้องมีไมโครโฟน
- OnlyOffice Document Server (Community License ควรใช้งานได้เพราะใช้ callCommand ภายใน plugin)

## หมายเหตุ

Plugin นี้ใช้ `callCommand` ภายใน plugin เพื่อ insert text ซึ่งควรทำงานได้กับ Community License เพราะไม่ต้องพึ่ง External Connector

## File Structure

```
speech-to-text/
├── config.json          # Plugin configuration
├── index.html           # Plugin UI
├── styles.css           # Styles
├── core.js              # Core utilities & editor functions
├── speech-recognition.js # Speech recognition logic
├── bootstrap.js         # Plugin initialization
└── resources/
    └── icon.svg         # Plugin icon
```
